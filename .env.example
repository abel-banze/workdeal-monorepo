# Copie para .env e ajuste se usar Postgres local do Windows
DATABASE_URL=postgres://postgres:202604@localhost:5432/workdeeal_monorepo
BETTER_AUTH_SECRET=change-me-min-32-chars-secret-replace-in-production
BETTER_AUTH_URL=http://localhost:4000
ALLOWED_ORIGINS=http://localhost:3000
NODE_ENV=development
PORT=4000
# Se usar o Postgres do Windows (C:\Program Files\PostgreSQL) em vez de Docker:
# DATABASE_URL=postgres://postgres:202604@localhost:5432/workdeeal_monorepo

# Google Maps — usado pela web (browser) e API (server) para Places, Geocoding e Maps JS
GOOGLE_MAPS_API_KEY=AIzaSyBFZ7mwas155hg0yUCegs3DGCKoYOzAUT8
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=AIzaSyBFZ7mwas155hg0yUCegs3DGCKoYOzAUT8

# Zernio WhatsApp — OTP via template verify_otp_usage (usado no onboarding)
# ZERNIO_PHONE_ID = account ID do WhatsApp no Zernio (usado como accountId na API)
ZERNIO_PHONE_ID=6a5dd51a3d50078deff9fc50
ZERNIO_API_KEY=sk_xxx

# Turbo.host SMS — OTP via SMS internacional (usa CODEBAZ como sender ID)
SMS_API_URL=https://my.turbo.host/api/international-sms/submit
SMS_USER_TOKEN=K0ZXRmlMdDVXZUc0aTlIbUJzL1pwUT09

# Resend Email — OTP via email
RESEND_API_KEY=re_xxx

# Cloudinary — upload de ficheiros (cotações e anexos)
# Usa os nomes canónicos abaixo. Alternativas aceites como fallback:
#   CLOUDINARY_URL (formato standard) ou aliases CLOUDINARY_NAME/API/SECRET
CLOUDINARY_CLOUD_NAME=xxx
CLOUDINARY_API_KEY=xxx
CLOUDINARY_API_SECRET=xxx
CLOUDINARY_FOLDER=workdeal
