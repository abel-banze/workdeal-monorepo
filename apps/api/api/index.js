// Vercel Serverless Function entry — moderno, detectado automaticamente por Vercel
// Este ficheiro apenas re-exporta o bundle já gerado por `pnpm run build` (dist/index.js)
// que contém todo o código da API + @workdeal/* inline via esbuild.
// Evita o problema de symlinks pnpm no runtime serverless.

export * from '../dist/index.js';
export { default } from '../dist/index.js';
